---
# Generated file. Do not edit directly.
# Source template: {{ template_name }}
dependencies: []
galaxy_info:
  role_name: grub
  namespace: guidugli
  author: guidugli
  company: Creuzo Tech
  description: >-
    Configure GRUB defaults, kernel command line options, and optional bootloader
    authentication while keeping release metadata aligned with the shared Molecule
    platform matrix.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
{% for platform in platforms %}
  - name: {{ platform.name }}
    versions:
{%   for version in platform.versions %}
      - {{ version | tojson }}
{%   endfor %}
{% endfor %}
  galaxy_tags:
    - grub
    - grub2
    - linux
    - bootloader
    - kernel
